# socketio_ex202606

Node.js와 Socket.IO로 만든 간단한 실시간 채팅 예제입니다.

## 실행

```bash
npm install
npm start
```

브라우저에서 `http://localhost:3000`으로 접속합니다.

PowerShell에서 `npm.ps1` 실행 정책 오류가 나면 다음처럼 실행합니다.

```bash
npm.cmd install
npm.cmd start
```

개발 중 자동 재시작을 사용하려면 다음 명령을 실행합니다.

```bash
npm run dev
```
